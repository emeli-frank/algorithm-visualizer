import 'package:flutter/material.dart';

class AStarVisualizer extends StatelessWidget {
  const AStarVisualizer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('A star algorithm visualization'),
    );
  }
}
